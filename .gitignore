.DS_Store

# Материалы безопасности — только локально, на GitHub не публикуются.
# Внутри рабочие эксплойты (security/pentest-*) и старая уязвимая сборка
# (security/archive/) — их нельзя выкладывать в открытый доступ.
security/
# ...и любой архив этой папки: `security/` закрывает каталог, но не файл
# security.zip рядом с ним, а в нём те же эксплойты. Ловим и его.
security.zip
security*.zip
*.zip

# Обёртка для сторов и стратегия монетизации — только локально, на GitHub не
# публикуются (в store/HOWTO.md лежат прикидки цен и дохода).
node_modules
store/

